import { createUseStyles } from 'react-jss';

export default createUseStyles({
  label: {
    textTransform: 'uppercase',
    fontSize: 10,
    borderRadius: 7,
    padding: 5,
    width: 'fit-content',
    fontWeight: 600,
  },
});
